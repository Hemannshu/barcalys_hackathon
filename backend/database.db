CREATE DATABASE face_auth_system;

USE face_auth_system;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    face_encoding BLOB NOT NULL,
    registered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);